name: task-2

on:
  push:
    branches:
      - main

jobs:
  job-a:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This is job-a"
  
  job-b:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This is job-b"

  job-c:
    needs: [job-b, job-a]
    runs-on: ubuntu-latest
    steps:
      - run: echo "This is job-c"

