name: hello-workflow

on:
    push:
        branches: [main]

jobs:
    job-a:
        runs-on: ubuntu-latest
        steps:
            - run: echo "job A running"
    job-b:
        runs-on: ubuntu-latest
        steps:
            - run: echo "Job B running"

