name: hello-workflow

on:
    push:
        branches: [main]

jobs:
    hello:
        runs-on: ubuntu-latest
        steps:
            - name: print-hello
              run: echo "Hellow from GHA"
    date:
        runs-on: ubuntu-latest
        steps:
            - name: print-date
              run: echo "20/7/2026"

