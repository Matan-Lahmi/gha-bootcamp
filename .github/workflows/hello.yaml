name: hello-workflow

on:
    push:
        branches: [main]

jobs:
    job-a:
        runs-on: ubuntu-latest
        steps:
            - name: job a
              run: echo "job A running"
            - name: job b
              run: echo "Job B running"

