name: task-5

on:
  workflow_dispatch:

jobs:
  job-a:
    strategy:
      matrix:
        env: [dev,staging,prod]
    runs-on: ubuntu-latest
    steps:
      - run: echo "Running on ${{ matrix.env }}"
