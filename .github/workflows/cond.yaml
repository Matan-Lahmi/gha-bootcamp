name: task1

on:
  push:
    branches:
      - main 

jobs:
  job-a:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/upload-artifact@4
        with: 
          name: myfile.txt
          path: /tmp
      - run: echo "Hello > myfile.txt"
