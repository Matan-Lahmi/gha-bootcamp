name: task1

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

jobs:
  job-a:
    runs-on: ubuntu-latest
    steps:
      - run: echo "this build is for version ${{ github.ref_name }}
