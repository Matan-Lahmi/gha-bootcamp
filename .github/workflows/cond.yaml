name: task1

on:
  push:
    branches:
      - main

jobs:
  job-a:
    strategy:
      matrix:
        env: ["prod","dev","staging"]
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ matrix.env }}"
      - if: matrix.env == 'prod'
        run: echo "This is prod environment"

