on:
  push:
    branches:
      - main 

jobs:
  job-a:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        env: ["dev","prod","staging"]
    env:
      APP_NAME: "myapp"
    steps:
      - run: echo "Deploying ${{ env.APP_NAME }} to ${{ matrix.env}}"
      - if: ${{ martix.env }} == 'prod'
        run: echo "Production deploying - extra caution"
