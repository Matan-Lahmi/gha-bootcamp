name: task1

on:
  push:
    branches:
      - main

jobs:
  job-a:
    runs-on: ubuntu-latest
    steps:
      - run: echo "MY_DYNAMIC_VAR=HelloFromStep1" >> $GITHUB_ENV
      - run: echo "The value is $MY_DYNAMIC_VAR"


