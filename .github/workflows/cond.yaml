name: task1

on:
  push:
    branches:
      - main 

jobs:
  job-a:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello" > myfile.txt
      - uses: actions/upload-artifact@v4
        with: 
          name: myfile
          path: myfile.txt

