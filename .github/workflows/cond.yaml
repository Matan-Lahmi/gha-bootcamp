name: task-5

on:
  push:
    branches:
      - main

jobs:
  job-a:
    if: github.ref == 'ref/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@4v
      - run: echo "Using this secret :${{ secret.MY_SECRET }}"
      - run: docker build -t myimage:${{ github.sha }} .
      - run: docker images
