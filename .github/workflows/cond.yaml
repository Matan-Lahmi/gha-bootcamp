name: task1

on:
  workflow_dispatch:
    inputs:
      env:
        type: choice
        options:
          - dev
          - staging
          - prod
        description: 'were to deploy?'
        required: true
        
        

jobs:
  job-a:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ github.event.inputs.env}}"


