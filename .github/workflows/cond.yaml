name: task3 

on:
  push:
    branches:
      - main 

jobs:
  job-a:
    runs-on: ubuntu-latest
    steps:
      - run: exit 1
  job-b:
    runs-on: ubuntu-latest
    steps:
      - run: echo "job b"
  job-c:
    runs-on: ubuntu-latest
    steps:
      - run: echo "job c"
  job-d:
    needs: [job-a, job-b, job-c]
    runs-on: ubuntu-latest
    steps:
      - run: echo "job d" 
