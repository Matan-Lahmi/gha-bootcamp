name: task3

on:
  workflow_dispatch:
    inputs:
      env:
        description: "select what env to test"
        required: true
        default: 'dev' 

jobs:
  job-a:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The user set the test on ${{ github.event.inputs.env }}"
