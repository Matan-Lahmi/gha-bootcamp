name: task1 

on: 
  push:
    branches:
      - main

jobs:
  job-a:
    runs-on: ubuntu-latest
    steps:
      - run: mkdir myapp
      - working-directory: myapp
        run: echo "123456" > file.txt
      - working-directory: myapp
        run: cat file.txt &&  pwd
      - run: pwd
      - run: cat file.txt

