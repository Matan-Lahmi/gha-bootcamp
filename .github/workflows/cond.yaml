name: task3

on:
  push:
    branches:
      - main 

jobs:
  job-a:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-python@v5
      - uses: actions/checkout@v4
      - run: python --version

