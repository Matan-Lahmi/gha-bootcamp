name: task1

on:
  push:
    branches:
      - main 

jobs:
  job-a:
    runs-on: ubuntu-latest
    steps:
      - run: exit 1

  job-b:
    needs: job-a
    if: always() 
    runs-on: ubuntu-latest
    steps:
      - run: echo "This always runs"
