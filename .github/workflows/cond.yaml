name: task1 

on: 
  push:
    branches:
      - main

jobs:
  job-a:
    runs-on: ubuntu-latest
    steps:
      - run: echo "VAR=hello" >> $GITHUB_ENV
      - run: echo "The value is ${{ env.VAR }}"
