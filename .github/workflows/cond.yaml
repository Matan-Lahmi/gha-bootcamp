name: task1

on:
  push:
    branches:
      - main
    
jobs:
  job-a:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello from job a"
      - run: exit 1
  
  job-b:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello from job b"
  
  job-c:
    runs-on: ubuntu-latest 
    steps:
      - run: echo "Hello from job c"
  
  job-d:
    needs: [job-a, job-b, job-c]
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello from job d"


