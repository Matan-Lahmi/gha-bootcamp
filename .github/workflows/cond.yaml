name: task1

on:
  push:
    branches:
      - main

jobs:
  job-a:
    runs-on: ubuntu-latest
    steps:
      - run: exit 1

  job-b:
    needs: job-a
    if: failure()
    runs-on: ubuntu-latest
    steps:
      - run: echo "Job a is failed!"
  
  job-c:
    needs: job-a
    runs-on: ubuntu-latest
    steps:
      - run: echo "job a pass"
  
