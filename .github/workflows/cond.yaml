name: task1

on:
  push:
    branches:
      - main

jobs:
  job-a:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker bulid -t myimage:${{ github.sha}} .
      - run: echo "${{ secrets.MY_SECRET }}"

