name: task1

on:
  push:
    branches:
      - main

jobs:
  job-a:
    runs-on: ubuntu-latest
    steps:
      - run: echo "my-name=Matan" >> $GITHUB_ENV
      - run: echo "my name is $my-name"

